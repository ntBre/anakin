use crate::{Dmat, Dvec};

/// Levenberg-Marquardt
/// HT = H + (L - 1)^2 * diag(H)
pub(crate) fn para_solver(l: usize) -> (Dvec, ()) {
    todo!();
}

pub(crate) fn hyper_solver(l: usize) -> (Dvec, ()) {
    todo!()
}
