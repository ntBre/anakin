pub(crate) struct OpenMM;

impl OpenMM {
    pub(crate) fn new() -> Self {
        Self
    }

    pub(crate) fn optimize(&self, shot: usize, align: bool) -> (f64, f64, f64) {
        todo!()
    }
}
