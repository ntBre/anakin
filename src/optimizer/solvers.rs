use crate::{Dmat, Dvec};

/// Levenberg-Marquardt
/// HT = H + (L - 1)^2 * diag(H)
pub(crate) fn para_solver(l: f64) -> (Dvec, f64) {
    todo!();
}

pub(crate) fn hyper_solver(l: f64) -> (Dvec, f64) {
    todo!()
}
